# Dependencies
node_modules
.pnpm-store

# Build
dist
.next
out
build
.turbo
*.tsbuildinfo

# Anchor / Rust
contracts/target
contracts/.anchor
**/*.rs.bk

# Env
.env
.env.local
.env.*.local

# Local key material (never commit)
apps/api/platform-wallet.json

# OS / IDE
.DS_Store
.idea
.vscode

# Debug
npm-debug.log*
pnpm-debug.log*

# Demo / test artifacts (devnet keypairs; do NOT commit)
scripts/.demo-wallets/
testing.md.local

# Local clone / reference only (nested repo)
wallet-adapter/
