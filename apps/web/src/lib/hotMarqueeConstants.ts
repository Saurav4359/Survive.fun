/** Linear scroll speed — px/s (time-based). */
export const HOT_MARQUEE_PX_PER_SEC = 52;

/** Integration clamp (seconds); keeps motion stable under long frames. */
export const HOT_MARQUEE_MAX_DT_SEC = 0.032;
