[package]
name = "survivefun"
version = "0.1.0"
description = "Survive.fun Anchor program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "survivefun"

[features]
no-entrypoint = []
no-idl-build = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []
# Enables 10s market duration for local integration tests only. Never deploy with this feature.
integration-test = []

[dependencies]
anchor-lang = { version = "0.31.1", features = ["init-if-needed"] }
