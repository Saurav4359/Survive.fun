# Prisma Studio resolves the query engine via Node; pnpm's isolated linker breaks that
# unless dependencies are hoisted like npm/yarn. This is the reliable fix for
# MODULE_NOT_FOUND / "Unable to resolve Prisma engine path" in Studio.
shamefully-hoist=true
